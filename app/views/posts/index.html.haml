%h3 POSTS

%table
  %thead
    %tr
      %th TITLE
      %th BODY
  %tbody
    - @posts.each do |post|
      %tr
        %td
          = link_to "#{post.title}", post_path(post)
        %td
          = post.body
