%h3 POSTS
= link_to "New Post", new_post_path
= link_to "View Logs", logs_path

%table
  %thead
    %tr
      %th TITLE
      %th BODY
  %tbody
    - @posts.each do |post|
      %tr
        %td
          = link_to "#{post.title}", post_path(post)
        %td
          = post.body
